# Base URL of the DBO backend API (see Plan 1 Task 15 / README).
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Set to 1 to serve committed fixtures from frontend/fixtures instead of
# calling the live API (offline development).
NEXT_PUBLIC_USE_MOCKS=1
